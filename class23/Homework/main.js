
console.log('———Intit Exercise O ———')

const n = 4


for (let i = 1; i <= (n / n); i++) {

    let row = ''

    for (let j = 1; j <= (n / n); j++) {
        row += ' '
    }

    for (let h = 1; h <= n; h++) {
        row += '*'
    }

    for (let k = 1; k <= (n / n); k++) {

        row += ' '
    }

console.log(row);
}

for (let i = 1; i <= n; i++) {

    let row = ''

    for (let j = 1; j <= (n / n); j++) {
        row += '*'
    }

    for (let h = 1; h <= n; h++) {
        row += ' '
    }

    for (let k = 1; k <= (n / n); k++) {

        row += '*'
    }

console.log(row);
}

for (let i = 1; i <= (n / n); i++) {

    let row = ''

    for (let j = 1; j <= (n / n); j++) {
        row += ' '
    }

    for (let h = 1; h <= n; h++) {
        row += '*'
    }

    for (let k = 1; k <= (n / n); k++) {

        row += ' '
    }

console.log(row);
}

console.log('———End———')